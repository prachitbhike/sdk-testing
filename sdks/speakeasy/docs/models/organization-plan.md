# OrganizationPlan

## Example Usage

```typescript
import { OrganizationPlan } from "openapi/models";

let value: OrganizationPlan = "free";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"free" | "pro" | "enterprise" | Unrecognized<string>
```