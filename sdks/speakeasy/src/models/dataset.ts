/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { ColumnSchema, ColumnSchema$inboundSchema } from "./column-schema.js";
import { DataSource, DataSource$inboundSchema } from "./data-source.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export const Format = {
  Csv: "csv",
  Parquet: "parquet",
  Json: "json",
} as const;
export type Format = OpenEnum<typeof Format>;

export const DatasetStatus = {
  Uploading: "uploading",
  Processing: "processing",
  Ready: "ready",
  Failed: "failed",
} as const;
export type DatasetStatus = OpenEnum<typeof DatasetStatus>;

export type Dataset = {
  id: string;
  projectId: string;
  name: string;
  format: Format;
  rowCount?: number | null | undefined;
  columns?: Array<ColumnSchema> | null | undefined;
  sizeBytes: number;
  status: DatasetStatus;
  createdAt: Date;
  source: DataSource;
};

/** @internal */
export const Format$inboundSchema: z.ZodMiniType<Format, unknown> = openEnums
  .inboundSchema(Format);

/** @internal */
export const DatasetStatus$inboundSchema: z.ZodMiniType<
  DatasetStatus,
  unknown
> = openEnums.inboundSchema(DatasetStatus);

/** @internal */
export const Dataset$inboundSchema: z.ZodMiniType<Dataset, unknown> = z.pipe(
  z.object({
    id: types.string(),
    project_id: types.string(),
    name: types.string(),
    format: Format$inboundSchema,
    row_count: z.optional(z.nullable(types.number())),
    columns: z.optional(z.nullable(z.array(ColumnSchema$inboundSchema))),
    size_bytes: types.number(),
    status: DatasetStatus$inboundSchema,
    created_at: types.date(),
    source: DataSource$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "project_id": "projectId",
      "row_count": "rowCount",
      "size_bytes": "sizeBytes",
      "created_at": "createdAt",
    });
  }),
);

export function datasetFromJSON(
  jsonString: string,
): SafeParseResult<Dataset, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Dataset$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Dataset' from JSON`,
  );
}
