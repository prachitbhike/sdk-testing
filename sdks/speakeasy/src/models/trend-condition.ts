/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export const Direction = {
  Increasing: "increasing",
  Decreasing: "decreasing",
  Either: "either",
} as const;
export type Direction = OpenEnum<typeof Direction>;

export type TrendCondition = {
  type: "trend";
  direction: Direction;
  slopeThreshold: number;
  windowSize: number;
};

/** @internal */
export const Direction$inboundSchema: z.ZodMiniType<Direction, unknown> =
  openEnums.inboundSchema(Direction);
/** @internal */
export const Direction$outboundSchema: z.ZodMiniType<string, Direction> =
  openEnums.outboundSchema(Direction);

/** @internal */
export const TrendCondition$inboundSchema: z.ZodMiniType<
  TrendCondition,
  unknown
> = z.pipe(
  z.object({
    type: types.literal("trend"),
    direction: Direction$inboundSchema,
    slope_threshold: types.number(),
    window_size: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "slope_threshold": "slopeThreshold",
      "window_size": "windowSize",
    });
  }),
);
/** @internal */
export type TrendCondition$Outbound = {
  type: "trend";
  direction: string;
  slope_threshold: number;
  window_size: number;
};

/** @internal */
export const TrendCondition$outboundSchema: z.ZodMiniType<
  TrendCondition$Outbound,
  TrendCondition
> = z.pipe(
  z.object({
    type: z.literal("trend"),
    direction: Direction$outboundSchema,
    slopeThreshold: z.number(),
    windowSize: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      slopeThreshold: "slope_threshold",
      windowSize: "window_size",
    });
  }),
);

export function trendConditionToJSON(trendCondition: TrendCondition): string {
  return JSON.stringify(TrendCondition$outboundSchema.parse(trendCondition));
}
export function trendConditionFromJSON(
  jsonString: string,
): SafeParseResult<TrendCondition, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TrendCondition$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TrendCondition' from JSON`,
  );
}
