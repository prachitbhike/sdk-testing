/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  ForecastPoint,
  ForecastPoint$inboundSchema,
} from "./forecast-point.js";

export type PredictionResult = {
  id: string;
  modelId: string;
  horizon: number;
  createdAt: Date;
  points: Array<ForecastPoint>;
};

/** @internal */
export const PredictionResult$inboundSchema: z.ZodMiniType<
  PredictionResult,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    model_id: types.string(),
    horizon: types.number(),
    created_at: types.date(),
    points: z.array(ForecastPoint$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "model_id": "modelId",
      "created_at": "createdAt",
    });
  }),
);

export function predictionResultFromJSON(
  jsonString: string,
): SafeParseResult<PredictionResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PredictionResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PredictionResult' from JSON`,
  );
}
